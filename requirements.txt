# Core dependencies
flask>=2.3.0,<4.0
requests>=2.28.0,<3.0
icalendar>=5.0.0,<6.0
python-dateutil>=2.8.0,<3.0

# Production server
gunicorn>=20.1.0,<22.0; platform_system != "Windows"

# Optional dependencies
# Install with: pip install -r requirements.txt -r requirements-dev.txt